---
time: 25
sidebar_position: 7
title: Proposal Lifecycle (simple-dao)
description: Model a simple-dao proposal as an explicit state machine — Pending → Active → Succeeded/Defeated → Executed, with Pending → Cancelled.
image: /img/soroban-social-card.png
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<PatternMeta
  slug="proposal-lifecycle"
  difficulty="intermediate"
  category="Governance"
  status="stable"
  lastReviewed="June 2026"
/>

## Overview

The `simple-dao` example uses a clear, auditable state machine so that proposals can only advance through valid transitions. This pattern encodes the full lifecycle as a `#[contracttype]` enum and enforces transitions at the contract boundary, making invalid states unrepresentable on-chain.

```text
Pending ──► Active ──► Succeeded ──► Executed
                  └──► Defeated
Pending ──► Cancelled
```

<PatternSection id="prerequisites" title="Prerequisites">

- Rust stable and `cargo`
- Soroban CLI installed ([setup guide](/docs/getting-started/setup))
- Familiarity with `contracttype` enums and `instance` storage

</PatternSection>

<PatternSection id="implementation" title="Implementation">

<PatternCallout variant="warning" title="Educational & Unaudited Example">

This pattern and its code examples are provided for educational purposes only and have not been security audited. Do not deploy or adapt this code directly in production environments without independent security reviews and comprehensive testing.

* **Reentrancy Limitations**: The illustrative contract below operates synchronously without external contract calls. However, production governance contracts (such as the full `simple-dao` example) that execute external calls during proposal execution must account for reentrancy and control-flow implications, ensuring state updates occur prior to external invocations or using reentrancy guards.
* **Storage TTL & Data Expiry**: The example relies on Soroban `instance` storage (`env.storage().instance()`), which is subject to ledger time-to-live (TTL) and data expiration. The example does not demonstrate TTL extension (`extend_ttl`). Production implementations must account for storage rent management so proposal state and voting data do not expire unexpectedly during active governance lifecycles.

</PatternCallout>

<PatternCallout variant="info" title="Design principle">

Encode every invalid state as a compile-time impossibility or a guarded `assert!`. Never rely on callers to pass the right state — the contract should enforce it.

</PatternCallout>

<Tabs>
  <TabItem value="contract" label="Contract" default>

```rust illustrative
#![no_std]
use soroban_sdk::{contract, contractimpl, contracttype, Env};

/// All valid proposal states.
#[contracttype]
#[derive(Clone, PartialEq, Debug)]
pub enum ProposalState {
    Pending,    // created but voting not open
    Active,     // voting is open
    Succeeded,  // more for-votes than against
    Defeated,   // equal or more against-votes
    Cancelled,  // proposer withdrew before voting opened
    Executed,   // on-chain action has been carried out
}

#[contracttype]
pub enum DataKey {
    State,
    VotesFor,
    VotesAgainst,
}

#[contract]
pub struct SimpleDao;

#[contractimpl]
impl SimpleDao {
    /// Create a new proposal in the Pending state.
    pub fn create(env: Env) {
        env.storage().instance().set(&DataKey::State, &ProposalState::Pending);
        env.storage().instance().set(&DataKey::VotesFor, &0_u32);
        env.storage().instance().set(&DataKey::VotesAgainst, &0_u32);
    }

    /// Pending → Active: open voting.
    pub fn activate(env: Env) {
        let state: ProposalState = env.storage().instance().get(&DataKey::State).unwrap();
        assert!(state == ProposalState::Pending, "must be Pending to activate");
        env.storage().instance().set(&DataKey::State, &ProposalState::Active);
    }

    /// Pending → Cancelled: withdraw the proposal before voting opens.
    pub fn cancel(env: Env) {
        let state: ProposalState = env.storage().instance().get(&DataKey::State).unwrap();
        assert!(state == ProposalState::Pending, "must be Pending to cancel");
        env.storage().instance().set(&DataKey::State, &ProposalState::Cancelled);
    }

    /// Record a vote while Active.
    pub fn vote(env: Env, in_favor: bool) {
        let state: ProposalState = env.storage().instance().get(&DataKey::State).unwrap();
        assert!(state == ProposalState::Active, "voting is not open");
        if in_favor {
            let v: u32 = env.storage().instance().get(&DataKey::VotesFor).unwrap_or(0);
            env.storage().instance().set(&DataKey::VotesFor, &(v + 1));
        } else {
            let v: u32 = env.storage().instance().get(&DataKey::VotesAgainst).unwrap_or(0);
            env.storage().instance().set(&DataKey::VotesAgainst, &(v + 1));
        }
    }

    /// Active → Succeeded or Defeated based on vote counts.
    pub fn finalize(env: Env) {
        let state: ProposalState = env.storage().instance().get(&DataKey::State).unwrap();
        assert!(state == ProposalState::Active, "must be Active to finalize");
        let for_votes: u32 = env.storage().instance().get(&DataKey::VotesFor).unwrap_or(0);
        let against_votes: u32 = env.storage().instance().get(&DataKey::VotesAgainst).unwrap_or(0);
        let next = if for_votes > against_votes {
            ProposalState::Succeeded
        } else {
            ProposalState::Defeated
        };
        env.storage().instance().set(&DataKey::State, &next);
    }

    /// Succeeded → Executed: carry out the on-chain action.
    pub fn execute(env: Env) {
        let state: ProposalState = env.storage().instance().get(&DataKey::State).unwrap();
        assert!(state == ProposalState::Succeeded, "must be Succeeded to execute");
        env.storage().instance().set(&DataKey::State, &ProposalState::Executed);
    }

    /// Get the current proposal state.
    pub fn get_state(env: Env) -> ProposalState {
        env.storage().instance().get(&DataKey::State).unwrap()
    }
}
```

  </TabItem>
  <TabItem value="tests" label="Tests">

```rust illustrative
#[cfg(test)]
mod tests {
    use super::*;
    use soroban_sdk::Env;

    fn setup() -> (Env, soroban_sdk::Address) {
        let env = Env::default();
        let id = env.register(SimpleDao, ());
        (env, id)
    }

    #[test]
    fn test_create_sets_pending() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        assert_eq!(client.get_state(), ProposalState::Pending);
    }

    #[test]
    fn test_pending_to_active() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        assert_eq!(client.get_state(), ProposalState::Active);
    }

    #[test]
    fn test_active_to_succeeded() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        client.vote(&true);
        client.vote(&true);
        client.vote(&false);
        client.finalize();
        assert_eq!(client.get_state(), ProposalState::Succeeded);
    }

    #[test]
    fn test_active_to_defeated() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        client.vote(&false);
        client.vote(&false);
        client.vote(&true);
        client.finalize();
        assert_eq!(client.get_state(), ProposalState::Defeated);
    }

    #[test]
    fn test_succeeded_to_executed() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        client.vote(&true);
        client.finalize();
        client.execute();
        assert_eq!(client.get_state(), ProposalState::Executed);
    }

    #[test]
    fn test_cancel_pending_proposal() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.cancel();
        assert_eq!(client.get_state(), ProposalState::Cancelled);
    }

    #[test]
    #[should_panic(expected = "must be Pending to cancel")]
    fn test_cannot_cancel_active_proposal() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        client.cancel();
    }

    #[test]
    #[should_panic(expected = "must be Succeeded to execute")]
    fn test_double_execute_panics() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        client.vote(&true);
        client.finalize();
        client.execute();
        client.execute();
    }

    #[test]
    #[should_panic(expected = "must be Pending to activate")]
    fn test_cannot_activate_twice() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        client.activate(); // invalid
    }

    #[test]
    #[should_panic(expected = "voting is not open")]
    fn test_cannot_vote_when_pending() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.vote(&true); // invalid
    }

    #[test]
    #[should_panic(expected = "must be Succeeded to execute")]
    fn test_cannot_execute_defeated_proposal() {
        let (env, id) = setup();
        let client = SimpleDaoClient::new(&env, &id);
        client.create();
        client.activate();
        client.vote(&false);
        client.finalize();
        client.execute(); // invalid
    }
}
```

  </TabItem>
</Tabs>

</PatternSection>

<PatternSection id="security" title="Security considerations">

- **Authorization**: Production DAOs must guard `activate`, `finalize`, and `execute` with `require_auth()` on a governor address or multi-sig, and `cancel` with the proposer's `require_auth()`. The example omits this for clarity.
- **Cancellation**: Only a `Pending` proposal can be cancelled; once voting opens, the proposal must go through `finalize` instead. In production, define who can cancel and enforce it with authorization.
- **Tie handling**: The current rule (`for > against`) treats ties as defeats. Make this explicit in your governance spec.
- **Double execution**: Once `Executed`, the state cannot regress; a second `execute` call panics. Add any further idempotency guards inside the `execute` body.
- **Reentrancy & External Calls**: State transition updates in the simplified example are synchronous. Production governance contracts executing arbitrary external calls (e.g., executing proposal payload actions) must update proposal state before invoking external contracts to prevent reentrancy or duplicate execution risks.
- **Storage TTL Management**: State stored in `instance` storage is subject to Soroban storage TTL rules. In production, ensure contract instance and proposal data entries are periodically extended via `extend_ttl` so data remains live throughout proposal lifecycles.
- **Sybil resistance**: Vote weighting (token-weighted, NFT-gated) must be added before production use.

<PatternCallout variant="warning" title="Production checklist">

Add `require_auth()` guards on every privileged transition before deploying to mainnet.

</PatternCallout>

</PatternSection>

<PatternSection id="related" title="Related patterns">

- [Authorization & Access Control](/docs/patterns/authorization) — guard transitions with `require_auth`
- [Governance Security](/docs/security/governance) — voting and timelock hardening
- [Lifecycle & Upgrades](/docs/patterns/lifecycle-upgrades) — contract upgrade patterns
- [Pattern library overview](/docs/patterns/overview)

</PatternSection>
